# Sample Go Project

A minimal Go project used as a sandbox integration test fixture.

## Usage

```bash
go build ./...
go test ./...
```

## Structure

- `cmd/` — entry point
- `internal/handler/` — HTTP handler
- `docs/` — documentation
- `secrets/` — credentials (do not commit)
